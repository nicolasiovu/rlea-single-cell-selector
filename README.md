# RLEA Single-Cell Selector Web App

Select boxes to upload from each unprocessed single-cell image

Plans: update UI because its a bit much

vibe coded, its basically a web version of the sorting app but with some box dragging functionality

 * as fair warning, yes over time vibe coding was used to polish these apps up, especially frontend / UI because I suck at those. these were meant to be little prototypes at the time and we just stuck with them, so i'd be open to rewriting stuff now that we want something proper.
